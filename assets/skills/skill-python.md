---
id: skill-python
title: Python Learning Notes
version: 1.0.0
tags: [python, venv, stdlib, list comprehension]
category: programming
author: aivos (Auto-generated by Knowledge Harvester)
lastUpdated: 2026-06-11
---

# Python 学习笔记

> 本模块为AI助手提供Python核心知识,包括语法、标准库、虚拟环境和最佳实践。
>
> *自动生成时间: 2026-06-11*

---

## 一、基础语法

### 1.1 变量和数据类型

Python是动态类型语言,无需声明变量类型:

```python
# 基本数据类型
name = "Alice"          # str
age = 25                # int
height = 1.75           # float
is_student = True       # bool
scores = [90, 85, 92]   # list
person = {"name": "Bob"}  # dict
unique_nums = {1, 2, 3}   # set
point = (3, 4)          # tuple
```

**注意事项**:
- 变量名区分大小写
- 使用小写字母和下划线命名 (`user_name`)
- 避免使用Python关键字作为变量名

### 1.2 字符串操作

```python
# 字符串格式化 (推荐f-string)
name = "World"
print(f"Hello, {name}!")           # Hello, World!
print("Hello, {}!".format(name))   # 旧式写法

# 常用方法
text = "  Hello World  "
text.strip()        # "Hello World" - 去除首尾空格
text.lower()        # "hello world"
text.upper()        # "HELLO WORLD"
text.replace("H", "h")  # "hello world"
text.split(" ")     # ["Hello", "World"]
"-".join(["a", "b"]) # "a-b"

# 切片
s = "Python"
s[0:2]    # "Py"
s[::-1]   # "nohtyP" - 反转
```

### 1.3 控制流

```python
# if/elif/else
score = 85
if score >= 90:
    grade = "A"
elif score >= 80:
    grade = "B"
else:
    grade = "C"

# for循环
for i in range(5):        # 0, 1, 2, 3, 4
    print(i)

for item in [1, 2, 3]:
    print(item)

# while循环
count = 0
while count < 5:
    count += 1

# break和continue
for i in range(10):
    if i == 5:
        break      # 跳出循环
    if i % 2 == 0:
        continue   # 跳过本次
```

---

## 二、数据结构

### 2.1 列表 (List)

```python
# 创建
fruits = ["apple", "banana", "cherry"]
numbers = list(range(10))

# 基本操作
fruits.append("orange")      # 追加
fruits.insert(0, "grape")    # 插入
fruits.remove("banana")      # 删除
item = fruits.pop()          # 弹出最后一个
fruits.sort()                # 排序
fruits.reverse()             # 反转

# 列表推导式 (重要!)
squares = [x**2 for x in range(10)]
evens = [x for x in range(20) if x % 2 == 0]
pairs = [(x, y) for x in [1,2] for y in [3,4]]

# 常用函数
len(fruits)       # 长度
min([1,2,3])      # 最小值
max([1,2,3])      # 最大值
sum([1,2,3])      # 求和
```

### 2.2 字典 (Dict)

```python
# 创建
person = {"name": "Alice", "age": 25}

# 访问
person["name"]            # "Alice"
person.get("email", "")   # "" - 安全访问,不存在返回默认值

# 操作
person["email"] = "alice@example.com"  # 添加/修改
del person["age"]                        # 删除

# 遍历
for key in person:
    print(key, person[key])

for key, value in person.items():
    print(key, value)

# 字典推导式
squares = {x: x**2 for x in range(5)}
# {0: 0, 1: 1, 2: 4, 3: 9, 4: 16}
```

### 2.3 集合 (Set)

```python
# 创建
unique = {1, 2, 3, 3, 3}  # {1, 2, 3} - 自动去重

# 集合运算
a = {1, 2, 3, 4}
b = {3, 4, 5, 6}
a & b    # {3, 4} - 交集
a | b    # {1, 2, 3, 4, 5, 6} - 并集
a - b    # {1, 2} - 差集
a ^ b    # {1, 2, 5, 6} - 对称差集
```

---

## 三、函数

### 3.1 定义和调用

```python
def greet(name, greeting="Hello"):
    """文档字符串 - 说明函数功能"""
    return f"{greeting}, {name}!"

# 调用
greet("Alice")                    # "Hello, Alice!"
greet("Bob", greeting="Hi")       # "Hi, Bob!"
greet(name="Charlie")             # 关键字参数
```

### 3.2 可变参数

```python
# *args - 接收任意数量的位置参数
def sum_all(*args):
    return sum(args)

sum_all(1, 2, 3, 4)  # 10

# **kwargs - 接收任意数量的关键字参数
def print_info(**kwargs):
    for key, value in kwargs.items():
        print(f"{key}: {value}")

print_info(name="Alice", age=25)
```

### 3.3 Lambda表达式

```python
# 匿名函数
square = lambda x: x ** 2
square(5)  # 25

# 配合sorted使用
students = [("Alice", 85), ("Bob", 92), ("Charlie", 78)]
students.sort(key=lambda x: x[1], reverse=True)
```

### 3.4 装饰器

```python
def timer(func):
    import time
    def wrapper(*args, **kwargs):
        start = time.time()
        result = func(*args, **kwargs)
        end = time.time()
        print(f"{func.__name__} took {end-start:.4f}s")
        return result
    return wrapper

@timer
def slow_function():
    time.sleep(1)
```

---

## 四、面向对象

### 4.1 类和对象

```python
class Dog:
    # 类属性
    species = "Canine"

    def __init__(self, name, age):
        # 实例属性
        self.name = name
        self.age = age

    def bark(self):
        return f"{self.name} says Woof!"

    def __str__(self):
        return f"Dog({self.name}, {self.age})"

# 使用
dog = Dog("Buddy", 3)
print(dog.bark())  # "Buddy says Woof!"
```

### 4.2 继承

```python
class Animal:
    def __init__(self, name):
        self.name = name

    def speak(self):
        raise NotImplementedError

class Cat(Animal):
    def speak(self):
        return f"{self.name} says Meow!"

class Dog(Animal):
    def speak(self):
        return f"{self.name} says Woof!"
```

### 4.3 属性装饰器

```python
class Temperature:
    def __init__(self, celsius):
        self._celsius = celsius

    @property
    def celsius(self):
        return self._celsius

    @celsius.setter
    def celsius(self, value):
        if value < -273.15:
            raise ValueError("Below absolute zero!")
        self._celsius = value

    @property
    def fahrenheit(self):
        return self._celsius * 9/5 + 32
```

---

## 五、模块和包

### 5.1 导入模块

```python
# 基本导入
import os
import sys
import json

# 导入特定函数
from os.path import join, exists
from collections import defaultdict, Counter

# 别名导入
import numpy as np
import pandas as pd

# 导入所有 (不推荐)
from math import *
```

### 5.2 常用标准库

```python
# os - 操作系统接口
import os
os.getcwd()              # 当前工作目录
os.listdir(".")          # 列出文件
os.path.join("a", "b")   # "a/b"
os.path.exists("file.txt")

# sys - 系统相关
import sys
sys.argv                 # 命令行参数
sys.exit(0)              # 退出程序

# json - JSON处理
import json
data = json.loads('{"name": "Alice"}')
json_str = json.dumps(data, indent=2)

# datetime - 日期时间
from datetime import datetime, timedelta
now = datetime.now()
tomorrow = now + timedelta(days=1)

# re - 正则表达式
import re
pattern = r"\d{3}-\d{4}"
re.match(pattern, "123-4567")
re.findall(r"\d+", "abc123def456")  # ["123", "456"]

# collections - 高级数据结构
from collections import Counter, defaultdict, deque
counter = Counter(["a", "b", "a", "c"])  # {"a": 2, "b": 1, "c": 1}
```

---

## 六、虚拟环境

### 6.1 创建和管理

```bash
# 创建虚拟环境
python -m venv myenv

# 激活 (Windows)
myenv\Scriptsctivate

# 激活 (macOS/Linux)
source myenv/bin/activate

# 退出
deactivate
```

### 6.2 包管理

```bash
# 安装包
pip install requests
pip install flask==2.0.1

# 查看已安装
pip list

# 导出依赖
pip freeze > requirements.txt

# 从文件安装
pip install -r requirements.txt

# 卸载
pip uninstall package_name
```

### 6.3 requirements.txt 示例

```
flask==2.0.1
requests>=2.25.0
numpy~=1.21.0
pandas
```

---

## 七、异常处理

```python
try:
    result = 10 / 0
except ZeroDivisionError as e:
    print(f"Error: {e}")
except Exception as e:
    print(f"Unexpected error: {e}")
else:
    print("No error occurred")
finally:
    print("Always executed")

# 自定义异常
class ValidationError(Exception):
    def __init__(self, message):
        self.message = message
        super().__init__(self.message)

raise ValidationError("Invalid input")
```

---

## 八、文件操作

### 8.1 读写文件

```python
# 读取整个文件
with open("file.txt", "r", encoding="utf-8") as f:
    content = f.read()

# 逐行读取
with open("file.txt", "r") as f:
    for line in f:
        print(line.strip())

# 写入文件
with open("output.txt", "w", encoding="utf-8") as f:
    f.write("Hello
World
")

# 追加模式
with open("log.txt", "a") as f:
    f.write("New log entry
")
```

### 8.2 CSV文件

```python
import csv

# 读取CSV
with open("data.csv", "r") as f:
    reader = csv.DictReader(f)
    for row in reader:
        print(row["name"], row["age"])

# 写入CSV
with open("output.csv", "w", newline="") as f:
    writer = csv.writer(f)
    writer.writerow(["Name", "Age"])
    writer.writerow(["Alice", 25])
```

---

## 九、最佳实践

### 9.1 PEP 8 编码规范

- 使用4空格缩进
- 每行不超过79字符
- 函数和类之间空两行
- 运算符两侧加空格: `a = b + c`
- 使用snake_case命名变量和函数
- 使用CamelCase命名类

### 9.2 常用技巧

```python
# 交换变量
a, b = b, a

# 链式比较
if 0 < x < 10:
    pass

# 三元表达式
result = "yes" if condition else "no"

# 解包
first, *rest = [1, 2, 3, 4, 5]

# 合并字典 (Python 3.9+)
merged = dict1 | dict2

# 枚举
for index, value in enumerate(items):
    print(index, value)

# 并行遍历
for name, age in zip(names, ages):
    print(name, age)
```

### 9.3 性能优化

```python
# 使用生成器而非列表 (节省内存)
sum(x**2 for x in range(1000000))

# 使用set进行快速查找
valid_users = set(users_list)
if user in valid_users:  # O(1)
    pass

# 使用join拼接字符串
"".join(str_list)  # 快于循环拼接

# 使用collections.deque实现队列
from collections import deque
queue = deque()
queue.appendleft(item)  # O(1)
queue.pop()             # O(1)
```

---

## 十、常见陷阱

1. **可变默认参数**
   ```python
   # ❌ 错误
   def append_to(item, lst=[]):
       lst.append(item)
       return lst

   # ✅ 正确
   def append_to(item, lst=None):
       if lst is None:
           lst = []
       lst.append(item)
       return lst
   ```

2. **浅拷贝 vs 深拷贝**
   ```python
   import copy
   original = [[1, 2], [3, 4]]
   shallow = original.copy()      # 嵌套对象仍共享引用
   deep = copy.deepcopy(original) # 完全独立副本
   ```

3. **浮点数精度**
   ```python
   0.1 + 0.2 == 0.3  # False!
   abs(0.1 + 0.2 - 0.3) < 1e-9  # 正确比较方式
   ```

---

*最后更新: 2026-06-11*
*生成工具: Knowledge Harvester v1.0.0*
